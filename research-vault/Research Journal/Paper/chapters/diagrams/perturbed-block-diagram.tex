The corresponding functional block-diagram of the adversarially perturbed
system is presented in Fig.~\ref{fig:attacked-system}. Compared with the unperturbed
system, the diagram introduces an additive
stochastic perturbation $\epsilon(k)$ on the output of the signal function
$\operatorname{sgn}(e(k))$, producing the perturbed signal
$\operatorname{sgn}(e(k)) + \epsilon(k)$
and the perturbed control input $\tilde{u}(k)$. All remaining blocks
are identical to those of the unperturbed system.
\begin{figure}[H]
	\centering
	\fbox{\parbox{0.65\textwidth}{\centering\vspace{3cm}%
		{\large [Perturbed-system block diagram placeholder]}%
		\vspace{3cm}}}
	\caption{The functional block-diagram of the discrete-time controlled system under adversarial perturbation described by \eqref{eq:attacked_system} and \eqref{eq:perturbed_closed_loop}.}
	\label{fig:attacked-system}
\end{figure}
