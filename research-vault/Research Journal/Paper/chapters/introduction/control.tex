\subsubsection*{\centering Control Function}

The MATLAB simulation from \cite{tymoshchuk2024tracking}
is designed to simulate the tracking control neural network. For that
purpose, it is necessary to define the control function, \control{$k$}, that will
be used to control the system~\autoref{systemModel}.
\begin{definition}
	Control Function \textsubscript{\cite{tymoshchuk2024tracking}}
	\begin{equation}
		u(k) = \begin{cases}
			g(x(k))^{-1} \bigl( -f(x(k)) + \Delta r(k) - \beta \text{sgn}(x(k) - r(k)) \bigr) & \text{if } g(x(k)) \neq 0 \\
			0                                                                                 & \text{if } g(x(k)) = 0
		\end{cases}
		\label{eq:control_function}
	\end{equation}

	such that, given a reference trajectory $r(k)$, the system~\autoref{systemModel} seeks to minimize the closed-loop error dynamics:

	\input{chapters/introduction/error.tex}

\end{definition}
