\noindent where $x(k) \in \mathfrak{R}^n$ is the discrete-time state variable,
$f(x(k)) \in \mathfrak{R}^n$ is the nonlinear function of system internal dynamics,
$g(x(k)) \in \mathfrak{R}^{n \times m}$ is the nonlinear function of system coupling dynamics,
$u(k) \in \mathfrak{R}^m$ is the control input applied to the system,
and $\tau = t(k+1) - t(k)$ is the discrete-time step, $k$.
