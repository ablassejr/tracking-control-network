where $x(k) \in \mathbb{R}^n$ is the discrete-time state variable,
$f(x(k)) \in \mathbb{R}^n$ is the nonlinear function of the system's internal state dynamics,
$g(x(k)) \in \mathbb{R}^{n \times m}$ is the nonlinear function that defines the effect of the
control input on the state dynamics,
$u(k) \in \mathbb{R}^m$ is the control input applied to the system,
and $\tau > 0$ is the discrete-time step.
