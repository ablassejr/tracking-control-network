% The formal convergence analysis has been replaced by the rounding
% description in voting-scheme.tex. The correctness of the defense follows
% directly from the rounding expression \eqref{eq:rounding_defense}:
% when |epsilon(k)| < 1/2, the rounding recovers sgn(e(k)) exactly,
% and the defended system equals the unperturbed system.
% Simulations below confirm this numerically.
