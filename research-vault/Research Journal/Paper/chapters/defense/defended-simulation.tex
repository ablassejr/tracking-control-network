\subsection{Simulation of Defended System}

The rounding defense~\eqref{eq:rounding_defense} is applied to the CSTR
system from \eqref{eq:cstr_dynamics}. Additive Gaussian noise with
$\sigma = 0.5$ is injected into the output of the signal function, the same
perturbation magnitude as in the attacked system. Because
$\sigma = \tfrac{1}{2}$, the sufficient condition
$|\epsilon(k)| < \tfrac{1}{2}$ holds at approximately $68\%$ of time steps.
During active tracking where
$\operatorname{sgn}(e(k)) \in \{-1, +1\}$, the rounding correctly recovers
the signal function value whenever the perturbation does not cross the
nearest decision boundary, yielding correct recovery at approximately
$84\%$ of time steps.

Fig.~\ref{fig:defended-state-tracking} presents the defended state trajectories.
The state variables $x_1(t)$ and $x_2(t)$ exhibit substantially improved
tracking relative to the undefended attacked system, confirming that the
rounding defense provides effective mitigation at the theoretical recovery
threshold.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.75\textwidth]{cxxplot_defended_time_vs_x1_r1}
	\caption{Trajectory of state $x_1(t)$ and reference $r_1(t)$ for the defended CSTR.}
	\label{fig:defended-state-tracking}
	\label{fig:defended-x1-r1}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.75\textwidth]{cxxplot_defended_time_vs_x2_r2}
	\caption{Trajectory of state $x_2(t)$ and reference $r_2(t)$ for the defended CSTR.}
	\label{fig:defended-x2-r2}
\end{figure}
